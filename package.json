{
  "name": "json-server",
  "version": "1.0.0",
  "description": "this is json server for travel book",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mansi Charmkar",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "json-serve": "^0.1.0",
    "json-server": "^0.17.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
